# Dockerized Chat Agent
## ✋ This sample has been generated automatically with a Nova Claude Skill. 

A complete example of a Nova chat agent containerized with Docker and deployed using **Docker Agentic Compose**.

```bash
docker compose up --build
```